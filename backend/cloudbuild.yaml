substitutions:
  _REGION: europe-west3
  _SERVICE: product-hub-backend
  _IMAGE: product-hub-backend
  _TAG: latest

options:
  substitutionOption: ALLOW_LOOSE

steps:
  # Quick syntax check to catch duplicate declarations / syntax errors
  - name: gcr.io/cloud-builders/npm
    dir: backend
    entrypoint: node
    args: ["--check", "index.js"]

  - name: gcr.io/cloud-builders/docker
    dir: .
    args:
      [
        'build',
        '-f',
        'Dockerfile',
        '-t',
        'gcr.io/${PROJECT_ID}/$_IMAGE:${_TAG}',
        '-t',
        'gcr.io/${PROJECT_ID}/$_IMAGE:latest',
        '.',
      ]

  # Run container locally and health-check port 8080 before pushing/deploying
  - name: gcr.io/cloud-builders/docker
    dir: .
    entrypoint: 'bash'
    args:
      - '-c'
      - |
        set -euo pipefail
        docker run --rm -d --name backend-test -e PORT=8080 -p 8080:8080 gcr.io/${PROJECT_ID}/$_IMAGE:${_TAG}
        success=""
        for i in {1..10}; do
          if curl -fsS http://localhost:8080/ >/dev/null; then
            success="yes"
            break
          fi
          sleep 2
        done
        docker logs backend-test || true
        docker stop backend-test
        if [ -z "$success" ]; then
          echo "Health check failed: backend did not respond on 8080"
          exit 1
        fi

  - name: gcr.io/cloud-builders/gcloud
    args:
      [
        'run',
        'deploy',
        '$_SERVICE',
        '--image',
        'gcr.io/${PROJECT_ID}/$_IMAGE:${_TAG}',
        '--memory',
        '3Gi',
        '--cpu',
        '2',
        '--min-instances',
        '1',
        '--region',
        '$_REGION',
        '--platform',
        'managed',
        '--allow-unauthenticated',
      ]

images:
  - 'gcr.io/${PROJECT_ID}/$_IMAGE:${_TAG}'
  - 'gcr.io/${PROJECT_ID}/$_IMAGE:latest'
